// import { createSelector } from 'reselect';
// const rootSelector = state => state;

// export const getRecipe = createSelector(
//   rootSelector,
//   ({ recipes }) => recipes
// );

export const getRegisterBool = state => state.handler.register;